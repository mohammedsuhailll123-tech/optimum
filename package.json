{
  "name": "takamul-root",
  "private": true,
  "engines": {
    "node": ">=18.0.0"
  },
  "workspaces": [
    "portfolio"
  ],
  "scripts": {
    "build": "npm run build --workspace=portfolio",
    "dev": "npm run dev --workspace=portfolio"
  },
  "dependencies": {
    "astro": "^7.0.3"
  }
}
